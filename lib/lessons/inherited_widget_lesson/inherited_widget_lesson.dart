import 'package:angela_yu_flutter_cource/export.dart';

class InheritedWidgetLesson extends StatelessWidget {
  static const id = 'InheritedWidgetLesson';
  const InheritedWidgetLesson({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('InheritedWidgetLesson'),
      ),
    );
  }
}
